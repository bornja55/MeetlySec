# Com Sec RAG Worker — โปรเซสแยกต่างหาก (ดู main.py docstring สำหรับเหตุผลสถาปัตยกรรม)
# ตัด streamlit/pandas/openpyxl ออกจาก requirements.txt ต้นฉบับของ Local RAG เพราะไม่ใช้ (ไม่มี UI
# ในโปรเซสนี้)
#
# **docx2txt กลับมาแล้ว (2026-08-07)** — เดิมตัดออกด้วยเพราะตอนนั้น rag_worker ไม่เคยอ่านไฟล์ .docx
# ตรงๆ เลย (โหลดแต่ FAISS index ที่ build ไว้ล่วงหน้าจาก storage/ เท่านั้น Local RAG เองก็ pre-convert
# เป็น .txt ก่อนด้วยสคริปต์แยกของตัวเอง ไม่เคยยื่น .docx ให้ SimpleDirectoryReader ตรงๆ) — พอเพิ่ม
# `confidential_rag.py::rebuild_index_from_corpus()` (session 3.36) ที่ยื่นไฟล์ .docx (BOD minutes ที่
# Approve แล้ว) เข้า `SimpleDirectoryReader` ตรงๆ เป็นครั้งแรก ได้ข้อความอ่านไม่ออก (garbled/binary)
# เข้าไปสร้างดัชนีจริง — พบจาก live test ผู้ใช้ (Policy Search หน้าเอกสารบอร์ด (ลับ) คืน sources เป็น
# ตัวอักษรมั่วทั้งหมด)
#
# **แก้ไขการวินิจฉัย (2026-08-07, session 3.39)**: ตอน session 3.37 เข้าใจผิดว่าขาดแค่ `docx2txt`
# เพียงอย่างเดียว — ติดตั้ง `docx2txt` แล้วปัญหา**ไม่หาย** ไล่จริงพบว่า `docx2txt.process()` เรียกตรงๆ
# อ่านไฟล์ได้ปกติ (ภาษาไทยอ่านออกครบ) แต่ `SimpleDirectoryReader` ยังคืนค่าเป็น raw ZIP bytes
# (`PK\x03\x04...` — .docx คือไฟล์ ZIP จริงๆ) แปลว่า llama_index **ไม่เคยเรียก `DocxReader` เลย** ตัว
# `DocxReader` เองไม่ได้อยู่ใน `llama-index-core` ตั้งแต่ llama_index แยก package เป็น
# `llama-index-readers-file` (v0.10+) — ไม่มี package นี้ = ไม่มี `.docx`/`.pdf` (แบบ non-simple)/ฯลฯ
# reader เฉพาะทางให้เลือกใช้เลยตั้งแต่ต้น ไม่เกี่ยวกับ `docx2txt` โดยตรง (`docx2txt` เป็นแค่
# dependency ที่ `DocxReader` เรียกใช้ *ถ้ามันถูกโหลดขึ้นมาได้ก่อน*) — ดู handoff.md session 3.39
# สำหรับ diagnostic เต็ม
docx2txt
llama-index-readers-file

# -- HTTP layer --
fastapi
uvicorn[standard]

# -- RAG framework (LlamaIndex) — เวอร์ชันต้องตรงกับที่ build ดัชนีของ Local RAG ไว้ --
llama-index-core
llama-index-embeddings-huggingface
llama-index-vector-stores-faiss
llama-index-llms-google-genai

# -- Embedding / reranker models + vector search engine --
# torch: อัปเดต 2026-08-02 — เปลี่ยนจากแผนเดิม "CPU-only เสมอ" (ดู task.md/handoff.md/PRD.md
# สำหรับเหตุผลเต็ม) เป็น **ใช้ GPU ถ้ามี** เพราะวัดจริงพบว่า reranker รันบน CPU + fp16 ช้ากว่า
# ปกติมาก (553s ต่อ 60 candidate) — main.py's _load_everything() detect GPU อัตโนมัติเองแล้ว
# (torch.cuda.is_available()) ถ้าไม่มี GPU จะ fallback เป็น CPU+fp32 ให้เอง (ไม่ใช้ fp16 บน CPU
# อีกต่อไป กัน regression เดิม) แต่**ต้องติดตั้ง torch แบบ CUDA เอง** (pip ธรรมดาจาก PyPI ได้แค่
# CPU-only wheel) รันคำสั่งนี้แทน `pip install torch` เฉยๆ (เวอร์ชัน/CUDA index เดียวกับที่
# Local RAG's venv ใช้อยู่แล้วบนเครื่องนี้ ยืนยันแล้วว่าใช้ได้จริง):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# ⚠️ ผลที่ตามมา: RAG worker นี้จะแย่ง VRAM กับ Module 2 (Diarization+ASR) ที่ยังไม่ได้สร้าง —
# ต้องออกแบบ GPU Lock ให้ครอบคลุมทั้งสองระบบตอนสร้าง Module 2 (ดู task.md)
torch
sentence-transformers
faiss-cpu==1.14.2

# -- Gemini API client --
google-genai

# -- Validation --
pydantic
